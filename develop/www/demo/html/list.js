define(['zepto','smExtend'],function($){
	$(".swiper-container").swiper({
    // pagination: '.pagination',
    // paginationClickable: true,//值为true时，点击分页器的指示点时会发生Swiper。
    freeMode : true,//滑动自由模式
    slidesPerView: 'auto'
  });

})
